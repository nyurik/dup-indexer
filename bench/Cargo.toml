[package]
name = "bench"
description = "Benchmarks to run with `cargo bench`"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
dup-indexer = { path = ".." }

[dev-dependencies]
criterion = "0.6"

[[bench]]
name = "bench"
harness = false
