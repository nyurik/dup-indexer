[package]
name = "bench"
description = "Benchmarks to run with `cargo bench`"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
dup-indexer = { path = ".." }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "bench"
harness = false
